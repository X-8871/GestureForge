# 项目总控

> 本文件是本项目的 AI 接手入口和稳定约束表；当前执行状态唯一维护在 `项目进程.md`。任何新一轮对话、换模型或中断后继续工作，都应先读取 Vault 根规则，再读取本文件与 `项目进程.md`，按任务需要读取 `项目总览.md`。

---

## 1. AI 接手协议

工程协作的唯一规范正文为 [[工程协作规范]]；所有总控、开发、审查窗口开始任务前必须读取。规范负责职责、目录、交付和验收，本文件保留项目稳定事实。

1. 先读取本文件，再读取 [[项目进程]]、[[项目总览]] 和当前相关的 `08-PlanMind` 计划。
2. 以用户当轮明确指令为最高优先级；本文件记录的是稳定事实，不替代用户的新要求。
3. 不能把“计划完成”写成“项目完成”。只有实际运行结果、截图、报告、代码或视频能够证明的内容，才可在 [[项目进程]] 中标记为完成。
4. 遇到硬件型号、接口、竞赛规则等未确认信息，标记为“待核对”，不得自行补全。
5. 修改工程代码前，先检查当前文件和已有改动；修改后说明改动范围和验证结果。
6. 已授权实施任务结束时，先刷新 [[项目进程]] 当前摘要和受影响模块状态，再追加有日期的证据与历史；只读审阅不写入。

## 2. 项目稳定事实

| 项目 | 当前信息 |
|---|---|
| 项目名称 | 基于 AX7Z020 的低比特神经网络 FPGA 加速系统，实现摄像头手势识别并驱动板卡进行实时交互 |
| 参赛方向 | FPGA 创新设计赛道中的自主选题赛道；组别待报名确认 |
| 目标平台 | ALINX AX7Z020，具体器件型号需以板卡实物和工程配置为准 |
| 核心任务 | 摄像头采集手势 → FPGA/PL 预处理与 INT8 推理 → PS 解析 → HDMI/LED 实时交互 |
| 暂定类别 | 握拳、手掌、数字一、数字二、数字三，共 5 类静态手势 |
| 当前队伍 | 用户与指导老师；其他队员以用户后续确认为准 |
| 作品目标 | 提交前达到国三与国二之间；初审通过后强化到国二与国一之间 |
| 论文安排 | 考研结束后再考虑，当前不占用作品开发主线 |
| 当前阶段 | 见 [[项目进程#1. 当前状态\|唯一当前状态]]；阶段日期不在本表重复维护 |
| 报名状态 | 尚未报名；需要准备选题方向和作品简介 |

### 竞赛提交要求摘要

- 必须提交完整工程、源码、仿真/验证结果、综合与实现报告、上板工程、运行脚本和实测输出。
- 工程目录和文件名使用英文、数字、下划线或连字符；中文只用于文档正文。
- 需要保存大模型协作记录，并将可复用经验整理为 `skill/` 技能包。
- 评分核心为创新与应用、性能与资源、功能完整性、AI 协作与 Skill、文档与复现；具体权重见 [[01-竞赛材料/赛道规则|赛道规则]]。

## 3. 当前系统边界

### 必须完成

- AX7Z020 工程能够建立、编译和上板运行。
- 完成一个可解释的低比特神经网络推理链路。
- 最终实现摄像头实时视频输入、PL 推理、识别结果与板卡交互闭环；固定图像仅作调试和阶段性验证。
- 提供精度、延迟、资源占用和运行稳定性的基本证据。
- 形成报名简介、作品说明、演示视频和答辩材料。

### 暂不扩张

- 不同时开发多个模型或多个应用场景。
- 不在基础版本阶段追求复杂摄像头算法、复杂 UI 或大模型。
- 不在作品提交前强行写论文。
- 不在没有测量数据前宣称“实时”“低功耗”或“显著加速”。

## 4. 文件与路径分工

实际路径见 [[工程入口]]，目录职责见 [[工程协作规范]]，本文件不重复维护。

## 5. 当前工作入口

按 [[项目进程]] 接手活动任务；用户操作顺序见 [[我的AI工程实验工作流]]。

## 当前用户决策（2026-09-10）

- **已确定**：采用强化方案；PL 负责图像预处理、卷积、池化及 INT8 主要计算；PS 负责任务调度、参数配置、读取识别结果、显示与交互控制。
- **已确定**：核心输入是摄像头实时视频。网络图片或 ESP32 下载图片不作为当前主线；固定图片仅用于离线调试、黄金参考和阶段性验证，不能代替最终摄像头闭环。
- **倾向，待硬件核实**：HDMI 摄像头 → AX7Z020 HDMI 输入 → FPGA 处理 → HDMI 输出 → 显示器。用户认为板卡有两个 HDMI 口，但端口数量/方向、板卡版本及摄像头 HDMI 输出能力均未完成证据核对，不能据此认定该链路已具备。
- **暂定**：五类静态手势为握拳、手掌、数字一、数字二、数字三。
- **暂定**：32×32 灰度图、INT8、两层轻量卷积＋激活/池化＋全连接，输出五类结果。通道数、卷积核、量化参数和时序接口仍需设计冻结。
- **已确定需要交互，形式待定**：HDMI 识别结果显示、LED、UART 类别/状态、按手势切换图片或显示模式均为候选，不要求全部实现。

以上为范围与方案决定，不代表实现、上板或性能验收通过。PS 的显示控制职责不代表已决定由 PS 搬运全部视频像素；视频收发、帧缓存与叠加实现待接口设计。

## 6. 当前技术决策

| 决策 | 内容 | 状态 |
|---|---|---|
| 应用任务 | 5 类静态手势识别并映射为板卡控制命令 | 暂定 |
| 模型路线 | 32×32 灰度、INT8、两层轻量卷积＋激活/池化＋全连接 | 暂定 |
| 部署路线 | PL：预处理、卷积、池化、INT8主要计算；PS：调度、配置、读取结果、显示与交互控制 | 已确定强化方案 |
| 视频链路 | 倾向 HDMI 摄像头→板卡 HDMI 输入→FPGA→HDMI 输出→显示器 | 端口方向及摄像头能力待核实 |
| 交互路线 | HDMI 结果显示、LED、UART、切图/显示模式 | 必须有交互，具体形式未定 |
| 验收策略 | 先完成国三—国二之间的稳定版本，再按初审结果强化 | 已确定 |

## 7. 关键入口

- [[工程协作规范]]
- [[项目进程]]
- [[项目总览]]
- [[工程入口]]
- [[我的AI工程实验工作流]]
- [[项目接手提示词]]
- [[04-提交材料/验收执行手册]]
