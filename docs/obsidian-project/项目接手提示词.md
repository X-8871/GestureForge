# FPGA 项目接手提示词

请接手 AX7Z020 低比特视觉识别项目。以下只定义读取路径，不复制项目实时进度。

## 读取顺序

1. Vault 根目录：`C:/Users/22061/Documents/Obsidian Vault`。先读 `AGENTS.md` 与 `00-AI全局整理规则.md`。
2. 在本项目目录读取 [[总控]]、[[项目进程]]、[[工程入口]]；以项目进程顶部当前摘要为准，历史记录按日期解释。
3. 按本轮任务读取 [[项目总览]]、对应方案或实验记录；不默认全读所有旧资料。
4. 需要排期时进入 [[08-PlanMind/00-INDEX]]，按南京时间定位当天计划；不存在就明确说明，不猜测任务完成。
5. 工程根目录以 [[工程入口]] 为准，修改前验证实际路径、文件、Git 状态和任务边界。不递归扫描 Vivado 生成目录。

## 本轮任务

用户在这里补充目标或在对话中直接说明。未给出任务时只总结接手状态，不自动执行历史待办。

## 输出与回填

先简要说明确认的状态、证据和本轮工作范围。实施后更新 [[项目进程]] 的当前摘要及相关模块，再追加历史；不能只在末尾写新结论而让顶部保留旧结论。

区分 AI 代运行、本人独立复现和真实上板。原始证据未归档就保留缺口；未验证工程不写成通过。按根规则验证、提交本轮相关代码并报告哈希，不能以交接代替继续完成已授权工作。
