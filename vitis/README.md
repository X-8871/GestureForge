# Vitis 工程区

PS 侧软件、驱动和启动工程放在这里。使用 Vivado 导出的 `.xsa` 建立 Vitis 平台后，再补充具体工程目录。

建议工作区：`D:\FPGA_Competition\vitis\`

Vitis 导出的 `.elf`、启动配置和可复现实验结果，按版本整理到根目录的 `build\bin\`；不要把 Vitis 自动生成的缓存目录复制到 Obsidian。
